# Backend API URL — set this in Vercel dashboard as an environment variable
# Leave empty in development (Vite proxy handles /api/* → localhost:5000)
VITE_API_URL=https://contest-remainder-2.onrender.com
